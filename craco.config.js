module.exports = {
  style: {
    postcss: {
      mode: "extends",
      plugins: [
        require('tailwindcss'),
        require('autoprefixer'),
        const path = require('path'),
  webpack:
    alias: {
      '@': path.resolve(__dirname, 'src'),
    }

      ]
    }
  },
};
